import { FormLevel } from '../components/form-level';

export const RegisterLevelsPage = () => {
  return (
    <main className="w-full flex flex-col items-center">
      <FormLevel />
    </main>
  );
};
